/**
 * Database types.
 *
 * Replaced in Phase 1 by the generated file:
 *   pnpm db:types  →  src/types/database.generated.ts
 *
 * Until the schema is applied there is nothing to generate from, so this
 * placeholder keeps the client factories typed without pretending to describe
 * tables that do not exist yet.
 */
export type Database = Record<string, never>
